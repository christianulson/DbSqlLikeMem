<Window x:Class="DbSqlLikeMem.VisualStudioExtension.UI.TestScenarioDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prop="clr-namespace:DbSqlLikeMem.VisualStudioExtension.Properties"
        Title="{x:Static prop:Resources.TestScenarioTitle}"
        Height="620"
        Width="960"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>
            <TextBlock VerticalAlignment="Center" Text="{x:Static prop:Resources.ScenarioNameLabel}" Margin="0,0,8,0" />
            <TextBox x:Name="ScenarioNameTextBox" Grid.Column="1" Margin="0,0,12,0" MinWidth="220" />
            <TextBlock Grid.Column="2" VerticalAlignment="Center" Text="{x:Static prop:Resources.TableLabel}" Margin="0,0,8,0" />
            <ComboBox x:Name="TableComboBox" Grid.Column="3" DisplayMemberPath="DisplayName" />
        </Grid>

        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock VerticalAlignment="Center" Text="{x:Static prop:Resources.FilterSqlLabel}" Margin="0,0,8,0" />
            <TextBox x:Name="FilterTextBox" Grid.Column="1" Margin="0,0,12,0" />
            <CheckBox x:Name="IncludeParentsCheckBox" Grid.Column="2" VerticalAlignment="Center" Content="{x:Static prop:Resources.IncludeParentsCheckbox}" Margin="0,0,12,0" />
            <Button Grid.Column="3" Content="{x:Static prop:Resources.LoadDataButton}" Padding="12,4" Click="OnLoadDataClick" />
        </Grid>

        <WrapPanel Grid.Row="2" Margin="0,0,0,8">
            <Button Content="{x:Static prop:Resources.SelectAllButton}" Padding="12,4" Margin="0,0,8,0" Click="OnSelectAllClick" />
            <Button Content="{x:Static prop:Resources.ClearAllButton}" Padding="12,4" Margin="0,0,8,0" Click="OnClearSelectionClick" />
        </WrapPanel>

        <DataGrid x:Name="RowsGrid"
                  Grid.Row="3"
                  AutoGenerateColumns="True"
                  CanUserAddRows="False"
                  SelectionMode="Extended"
                  IsReadOnly="False" />

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="{x:Static prop:Resources.ExtractScenarioButton}" Padding="14,5" Margin="0,0,8,0" Click="OnExtractClick" />
            <Button Content="{x:Static prop:Resources.CloseButton}" Padding="14,5" Click="OnCloseClick" />
        </StackPanel>
    </Grid>
</Window>
