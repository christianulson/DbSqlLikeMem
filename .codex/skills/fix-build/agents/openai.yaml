interface:
  display_name: "Fix Build"
  short_description: "Resolve build and compiler issues"
  default_prompt: "Use $fix-build to resolve build errors and warnings with minimal safe code changes."

policy:
  allow_implicit_invocation: true
