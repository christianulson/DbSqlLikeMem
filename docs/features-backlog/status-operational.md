# Status operacional do backlog

Objetivo: registrar andamento de sprint, próximos passos imediatos e riscos de execução sem inflar o `index.md` com detalhe volátil.

## Como usar

- Atualize este arquivo para status de sprint, lote atual e decisões operacionais.
- Mantenha `docs/features-backlog/index.md` como visão arquitetural e percentual consolidado.
- Quando um percentual do índice mudar, registre a evidência correspondente usando `docs/features-backlog/progress-update-checklist.md`.

## Sprint / ciclo atual

- Foco principal: consolidar governança de release, documentação para consumidores e automações cross-dialect sem duplicar lógica já estabilizada no parser/runtime, enquanto fechamos gaps explícitos remanescentes do `SqlAzure` na camada Strategy e melhoramos a paridade de geração entre VSIX e VS Code com baseline versionada de templates no próprio repositório, aplicação direta nas duas extensões, defaults operacionais coerentes por perfil, consumo desses perfis também no `Configure Mappings` da VSIX, escopo correto de mapeamento por conexão/tipo, suporte operacional completo a `Sequence` no VS Code, metadado explícito de revisão periódica, validação de tokens suportados, snapshot padronizado `// DBSqlLikeMem:*` nos artefatos gerados, agora com estrutura mínima quando disponível no VS Code inclusive para `Sequence`, comparação estrutural também entre artefatos complementares na VSIX, regra objetiva de revisão vencida (`nextPlannedReviewOn`) tanto nos resumos das extensões quanto no auditor de release, exposição direta dos diretórios recomendados de saída do perfil no próprio resumo operacional e alinhamento do gate documental ao caminho canônico da wiki em submódulo (`docs/Wiki`).
- Foco principal: consolidar governança de release, documentação para consumidores e automações cross-dialect sem duplicar lógica já estabilizada no parser/runtime, enquanto fechamos gaps explícitos remanescentes do `SqlAzure` na camada Strategy e melhoramos a paridade de geração entre VSIX e VS Code com baseline versionada de templates no próprio repositório, aplicação direta nas duas extensões, defaults operacionais coerentes por perfil, consumo desses perfis também no `Configure Mappings` da VSIX, escopo correto de mapeamento por conexão/tipo, suporte operacional completo a `Sequence` no VS Code, metadado explícito de revisão periódica, validação de tokens suportados, snapshot padronizado `// DBSqlLikeMem:*` nos artefatos gerados, agora com estrutura mínima quando disponível no VS Code inclusive para `Sequence`, comparação estrutural também entre artefatos complementares na VSIX, regra objetiva de revisão vencida (`nextPlannedReviewOn`) tanto nos resumos das extensões quanto no auditor de release, exposição direta dos diretórios recomendados de saída do perfil no próprio resumo operacional, alinhamento do gate documental ao caminho canônico da wiki em submódulo (`docs/Wiki`), contrato verificável de release notes/limitações abertas no `CHANGELOG.md` e nos guias de publicação, validação pós-pack da versão efetivamente publicada no `.nuspec`/`.nupkg` e entrada única do runner cross-dialect agora também para a camada `Strategy`.
- Itens em andamento: endurecimento dos gates de metadados NuGet a partir do `Directory.Build.props`, validação de SemVer, integridade mínima das extensões, alinhamento de compatibilidade VSIX, governança de localização do pacote VS Code, integração dos workflows de publicação, governança documental do backlog, alinhamento dos READMEs/guias/wiki de entrada, contrato explícito de versões/tags por artefato, fechamento da trilha `parser` para `SqlAzure`, cobertura comportamental de trigger em `SqlAzure`, cobertura transacional/lifecycle em `SqlAzure`, paridade de tokens/template entre VSIX e VS Code, scaffold útil de geração de testes no VS Code, consistência real do trio `teste/model/repository`, detecção explícita de drift pelo snapshot `// DBSqlLikeMem:*` agora nas duas extensões, incluindo estrutura mínima (`Columns`/`ForeignKeys`) no VS Code quando disponível, metadata de `Sequence` (`StartValue`/`IncrementBy`/`CurrentValue`) no provider SQL Server da extensão e comparação estrutural de `Model`/`Repository` contra a classe principal na VSIX, detalhamento persistido dos artefatos faltantes no VS Code, distinção explícita de trio local incompleto na VSIX, tooltip de diagnóstico por objeto na árvore da VSIX, baseline versionada `api`/`worker` em `templates/dbsqllikemem/vCurrent`, aplicação direta desses perfis nas duas extensões, defaults recomendados de mapeamento de testes no VS Code, defaults equivalentes por perfil/tipo agora também no `Configure Mappings` da VSIX, resumo compartilhado de descrição/foco/revisão da baseline nos diálogos da VSIX, contexto equivalente de revisão/drift agora também nos quick picks do VS Code, escopo correto de `Configure Mappings` na VSIX, suporte operacional a `Sequence` no manager/árvore/mappings do VS Code, metadado versionado de revisão periódica, padrão configurável de nome de arquivo para `Model`/`Repository` nas duas extensões, validação de tokens suportados em templates customizados, detecção automática de placeholders inválidos nas baselines versionadas e revisão dos percentuais/documentação correlatos.
- Próximo incremento: usar os auditores novos como base para fechar gaps restantes de publicação/extensões e voltar a priorizar ondas técnicas de parser/executor/confiabilidade com o `SqlAzure` já coberto de forma explícita em parser e estratégia transacional, mantendo a trilha de geração das extensões alinhada por token/capacidade, baseline versionada, checklist de revisão, metadado de cadência explícito, defaults operacionais coerentes, consumo homogêneo de baseline também nos mappings da VSIX e nos quick picks do VS Code, resumo compartilhado de perfil no ponto de uso, escopo de mapping consistente, suporte homogêneo a `Sequence`, tooltip diagnóstico consistente, uso operacional coerente nas duas extensões, hard stop automático quando a revisão trimestral da baseline vencer e hardening incremental dos checkers Python já colocados no CI como gate rápido de governança.

## Evidência recente

- Tests/files alterados: `scripts/check_release_readiness.py`, `scripts/check_cross_dialect_snapshot_format.py`, `scripts/check_slnx_project_coverage.py`, `scripts/test_check_cross_dialect_snapshot_format.py`, `scripts/test_check_release_readiness.py`, `scripts/test_check_slnx_project_coverage.py`, `scripts/check_nuget_package_metadata.py`, `scripts/test_check_nuget_package_metadata.py`, `.github/workflows/provider-test-matrix.yml`, `.github/workflows/nuget-publish.yml`, `.github/workflows/vsix-publish.yml`, `.github/workflows/vscode-extension-publish.yml`, `scripts/run_cross_dialect_equivalence.sh`, `scripts/refresh_cross_dialect_snapshots.sh`, `docs/cross-dialect-strategy-snapshot.md`, `README.md`, `src/README.md`, `docs/getting-started.md`, `docs/old/providers-and-features.md`, `docs/Wiki/Home.md`, `docs/Wiki/Getting-Started.md`, `docs/Wiki/Publishing.md`, `docs/Wiki/Providers-and-Compatibility.md`, `docs/info/multi-target-compat-audit.md`, `docs/publishing.md`, `docs/README.md`, `docs/nuget-readiness-validation-report.md`, `CHANGELOG.md`, `eng/visualstudio/PublishManifest.json`, `templates/dbsqllikemem/README.md`, `templates/dbsqllikemem/review-checklist.md`, `templates/dbsqllikemem/review-metadata.json`, `templates/dbsqllikemem/vCurrent/api/model.template.txt`, `templates/dbsqllikemem/vCurrent/api/repository.template.txt`, `templates/dbsqllikemem/vCurrent/worker/model.template.txt`, `templates/dbsqllikemem/vCurrent/worker/repository.template.txt`, `templates/dbsqllikemem/vNext/README.md`, `src/DbSqlLikeMem.SqlAzure/SqlAzureProviderMocks.cs`, `src/DbSqlLikeMem.SqlAzure.Test/Parser/SqlAzureDialectFeatureParserTests.cs`, `src/DbSqlLikeMem.SqlAzure.Test/Strategy/SqlAzureTriggerStrategyTests.cs`, `src/DbSqlLikeMem.SqlAzure.Test/Strategy/SqlAzureTransactionStrategyTests.cs`, `src/DbSqlLikeMem.VisualStudioExtension.Core/Generation/ClassGenerator.cs`, `src/DbSqlLikeMem.VisualStudioExtension.Core/Generation/TemplateBaselineCatalog.cs`, `src/DbSqlLikeMem.VisualStudioExtension.Core/Generation/TemplateBaselineGovernance.cs`, `src/DbSqlLikeMem.VisualStudioExtension.Core/Generation/TemplateBaselinePresentation.cs`, `src/DbSqlLikeMem.VisualStudioExtension.Core/Generation/TemplateBaselineProfile.cs`, `src/DbSqlLikeMem.VisualStudioExtension.Core/Generation/TemplateContentRenderer.cs`, `src/DbSqlLikeMem.VisualStudioExtension.Core/Generation/TemplateReviewMetadata.cs`, `src/DbSqlLikeMem.VisualStudioExtension.Core/Generation/TemplateReviewMetadataReader.cs`, `src/DbSqlLikeMem.VisualStudioExtension.Core/Generation/TemplateTokenCatalog.cs`, `src/DbSqlLikeMem.VisualStudioExtension.Core/Models/ObjectTypeMapping.cs`, `src/DbSqlLikeMem.VisualStudioExtension.Core/Models/ObjectHealthResult.cs`, `src/DbSqlLikeMem.VisualStudioExtension.Core/Models/ObjectHealthStatus.cs`, `src/DbSqlLikeMem.VisualStudioExtension.Core/Services/ConnectionMappingService.cs`, `src/DbSqlLikeMem.VisualStudioExtension.Core/Validation/ObjectConsistencyChecker.cs`, `src/DbSqlLikeMem.VisualStudioExtension.Core/README.md`, `src/DbSqlLikeMem.VisualStudioExtension.Core.Test/ClassGeneratorTests.cs`, `src/DbSqlLikeMem.VisualStudioExtension.Core.Test/ConnectionMappingServiceTests.cs`, `src/DbSqlLikeMem.VisualStudioExtension.Core.Test/ObjectConsistencyCheckerTests.cs`, `src/DbSqlLikeMem.VisualStudioExtension.Core.Test/TemplateBaselineCatalogTests.cs`, `src/DbSqlLikeMem.VisualStudioExtension.Core.Test/TemplateBaselineGovernanceTests.cs`, `src/DbSqlLikeMem.VisualStudioExtension.Core.Test/TemplateBaselinePresentationTests.cs`, `src/DbSqlLikeMem.VisualStudioExtension.Core.Test/TemplateReviewMetadataReaderTests.cs`, `src/DbSqlLikeMem.VisualStudioExtension.Core.Test/TemplateContentRendererTests.cs`, `src/DbSqlLikeMem.VisualStudioExtension.Core.Test/StatePersistenceServiceTests.cs`, `src/DbSqlLikeMem.VisualStudioExtension.Core.Test/TemplateTokenCatalogTests.cs`, `src/DbSqlLikeMem.VsCodeExtension/.vscodeignore`, `src/DbSqlLikeMem.VsCodeExtension/package.json`, `src/DbSqlLikeMem.VsCodeExtension/README.md`, `src/DbSqlLikeMem.VsCodeExtension/src/generation-support.ts`, `src/DbSqlLikeMem.VsCodeExtension/src/template-baselines.ts`, `src/DbSqlLikeMem.VsCodeExtension/src/template-token-catalog.ts`, `src/DbSqlLikeMem.VsCodeExtension/src/extension.ts`, `src/DbSqlLikeMem.VsCodeExtension/tests/generation-support.test.js`, `src/DbSqlLikeMem.VsCodeExtension/tests/template-baselines.test.js`, `src/DbSqlLikeMem.VsCodeExtension/tests/template-token-catalog.test.js`, `src/DbSqlLikeMem.VisualStudioExtension/README.md`, `src/DbSqlLikeMem.VisualStudioExtension/UI/DbSqlLikeMemToolWindowViewModel.cs`, `src/DbSqlLikeMem.VisualStudioExtension/UI/DbSqlLikeMemToolWindowControl.xaml`, `src/DbSqlLikeMem.VisualStudioExtension/UI/DbSqlLikeMemToolWindowControl.xaml.cs`, `src/DbSqlLikeMem.VisualStudioExtension/UI/ExplorerNode.cs`, `src/DbSqlLikeMem.VisualStudioExtension/UI/MappingDialog.xaml`, `src/DbSqlLikeMem.VisualStudioExtension/UI/MappingDialog.xaml.cs`, `src/DbSqlLikeMem.VisualStudioExtension/UI/TemplateConfigurationDialog.xaml`, `src/DbSqlLikeMem.VisualStudioExtension/UI/TemplateConfigurationDialog.xaml.cs`, `src/DbSqlLikeMem.VisualStudioExtension/source.extension.vsixmanifest`, `src/DbSqlLikeMem.VisualStudioExtension/DbSqlLikeMem.VisualStudioExtension.csproj`.
- Providers cobertos: trilha de publicação/governança; impacto transversal em NuGet, VSIX e VS Code extension.
- Comando planejado ou executado: planejado para a rodada final `python3 scripts/check_release_readiness.py` e `python3 scripts/check_nuget_package_metadata.py --artifacts-dir ./artifacts` junto dos demais checks consolidados.
- Resultado: baseline de release passa a ter auditoria objetiva pré-pack e pós-pack, incluindo formato SemVer do core e das extensões, coerência básica dos TFMs documentados ao consumidor, agora também protegida no guia secundário `docs/old/providers-and-features.md`, integridade mínima dos pacotes de extensão, alinhamento de compatibilidade declarada da VSIX, governança de placeholders/localização do pacote VS Code, gate direto nos workflows de publicação, agora também no próprio fluxo NuGet via `scripts/check_release_readiness.py` antes do `restore`, validação ampliada do `.nuspec` a partir do `Directory.Build.props`, agora também cobrindo `requireLicenseAcceptance`, verificação mínima do checklist/template de governança documental, alinhamento dos READMEs/guias/wiki de entrada, marcação explícita do relatório estático de multi-target como artefato histórico, cobertura da wiki espelhada de publicação/compatibilidade, vigilância dos READMEs operacionais das próprias extensões, agora também com o contrato `workflow -> fonte de versão -> publish` explícito e auditado no próprio README, contrato explícito de versão/tag por artefato, agora também materializado e auditado dentro dos próprios workflows de publish, incluindo o Environment resolvido do publish NuGet via `vars.NUGET_PUBLISH_ENVIRONMENT` com fallback `nuget-publish`, cobertura dos índices de descoberta (`docs/README.md`/`docs/Wiki`), fechamento da trilha `parser` para `SqlAzure` com suíte dedicada, incluindo ordered-set `STRING_AGG`, cobertura comportamental de trigger no provider Azure, cobertura explícita de estratégia transacional/lifecycle (`Close`/`Open`, savepoint e reset volátil) no mesmo provider, entrada única do runner cross-dialect agora também para `Strategy`, com placeholder versionado, validação estrutural, job dedicado no CI e unittest Python cobrindo o contrato do snapshot, hardening do checker de cobertura da solução `.slnx` para normalizar barras no XML e evitar falso positivo entre Windows e CI Linux, paridade ampliada de geração entre VSIX/VS Code com suporte a `{{Namespace}}`, persistência do namespace no mapeamento e uso do mesmo contexto também no `FileNamePattern` (`{Namespace}`), scaffold inicial útil e teste local mínimo na extensão VS Code com consistência real do trio `teste/model/repository`, snapshot padronizado `// DBSqlLikeMem:*` agora também em `teste/model/repository` para rastreabilidade comum entre as duas extensões, detecção explícita de drift de artefato agora tanto no VS Code quanto na VSIX, detalhamento persistido dos artefatos faltantes no próprio nó da árvore, defaults de mapeamento de testes coerentes com os perfis `api/worker` na extensão VS Code, consumo equivalente desses perfis também pelo `Configure Mappings` da VSIX via catálogo central, distinção explícita de trio local incompleto na VSIX, tooltip diagnóstico por objeto com lista determinística de artefatos faltantes na árvore da VSIX, resumo compartilhado de descrição/foco/revisão/recomendação da baseline diretamente nos diálogos da VSIX, agora alimentado também pelo `review-metadata.json` quando a baseline versionada está disponível no ambiente, e consumo equivalente desse mesmo metadata também nos quick picks de baseline do VS Code, com exibição de última revisão, próxima janela, evidências e drift de governança; baseline versionada `api`/`worker` em `templates/dbsqllikemem/vCurrent` segue com validação automática no auditor de release e consumo operacional direto nas duas extensões, além de checklist formal de revisão periódica, metadado versionado de cadência/última revisão/próxima janela em `review-metadata.json`, validação explícita de tokens suportados em templates customizados, padrão configurável de nome de arquivo para `Model`/`Repository` nas duas extensões e detecção automática de placeholders inválidos nas próprias baselines; placeholder remanescente de publisher VSIX segue mapeado como gap explícito até o modo estrito de publish.

## Riscos e bloqueios

- Risco atual: publicação de extensões ainda depende de credenciais/identidade final de marketplace fora do código.
- Mitigação: auditor registra warning explícito para placeholder restante e valida automaticamente o restante da cadeia objetiva de release.
- Gap mantido intencionalmente: `publisher` do `eng/visualstudio/PublishManifest.json` não foi inventado no código sem uma definição operacional confirmada.
