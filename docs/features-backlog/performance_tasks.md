| Nº | Descrição curta                       | Detalhes da atividade (resumidos)                                                                                                                                         | Arquivo(s) alvo principal                                    | Status |
| -: | ------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------ | ------ |
|  1 | Mapear armazenamento e índices atuais | Revisar o código de `TableMock` para entender como `_items` e os dicionários de índices são gerenciados; documentar os pontos de atualização.                             | `TableMock.cs`                                               | OK     |
|  2 | Criar índice de PK via dicionário     | Adicionar em `TableMock` um `Dictionary<PK, int>` para mapear a chave primária ao índice da linha. Atualizar esse dicionário em `Add`, `UpdateRowColumn` e `RemoveAt`.    | `TableMock.cs`                                               | 0 %    |
|  3 | Ajustar operações PK                  | Refatorar `DbDeleteStrategy`, `DbUpdateStrategy` e seleções para usar o dicionário de PK ao invés de varreduras.                                                          | `DbDeleteStrategy.cs`, `DbUpdateStrategy.cs`, query executor | 0 %    |
|  4 | Atualização incremental de índices    | Introduzir um mecanismo de marcação (“dirty flags”) que adie a reconstrução total dos índices; atualizar apenas a parte alterada e reconstruir em lote quando necessário. | `TableMock.cs`                                               | 0 %    |
|  5 | Implementar batch insert/upsert       | Criar método dedicado para inserir múltiplas linhas de uma só vez, atualizando os índices em bloco. Refatorar `DbInsertStrategy` para aproveitar esse método.             | `DbInsertStrategy.cs`                                        | 0 %    |
|  6 | Cache de AST e planos LINQ            | Adicionar cache (ex.: `ConcurrentDictionary`) que mapeie a expressão LINQ ou SQL para o AST e delegados de execução. Utilizar o cache em `SqliteTranslator`.              | `SqliteTranslator.cs`                                        | 0 %    |
|  7 | Cache de acesso a propriedades        | Gerar e armazenar delegados de leitura/gravação via `Expression` para cada tipo de entidade, eliminando `GetProperty` repetitivo.                                         | Camada de materialização                                     | 0 %    |
|  8 | Otimizar comandos `RETURNING`         | Alterar `SqliteCommandMock` para capturar somente as colunas retornadas em vez de clonar objetos completos; minimizar trabalho extra em `RETURNING`.                      | `SqliteCommandMock.cs`                                       | 0 %    |
|  9 | Otimizar JSON                         | Implementar leitor baseado em `Utf8JsonReader`/`JsonDocument`; compilar caminhos JSON em delegados; armazenar cache de caminhos frequentes.                               | Funções JSON                                                 | 0 %    |
| 10 | Otimizar temporais                    | Criar mapeamento fixo para tokens de `DATEADD`, `DATEDIFF` etc.; utilizar aritmética direta de `DateTime`/`TimeSpan` sem conversões.                                      | Funções temporais                                            | 0 %    |
| 11 | Melhorar agregações de string         | Pré-alocar `StringBuilder` com tamanho estimado; usar `HashSet` para `DISTINCT`; acumular grupos grandes em estruturas reutilizáveis.                                     | Agregadores de string                                        | 0 %    |
| 12 | Otimizar funções de janela            | Reutilizar ordenações já calculadas; manter contadores incrementais para `ROW_NUMBER` e pré-calcular deslocamentos de `LAG`.                                              | Funções de janela                                            | 0 %    |
| 13 | Log de alterações para transações     | Implementar journal de alterações em vez de clonar linhas completas em savepoints. Registrar inserções/updates/deletes com versões antigas e aplicar no rollback.         | Lógica de transações                                         | 0 %    |
| 14 | Monitoramento e logs                  | Inserir métricas de tempo em pontos críticos (parsing, atualização de índices, reflexão); configurar logs para análise de performance contínua.                           | Vários                                                       | 0 %    |
| 15 | Suíte de benchmarks automatizada      | Expandir o projeto de testes de performance para rodar automaticamente; medir cada otimização para evitar regressões.                                                     | Pasta de benchmarks                                          | 0 %    |
| 16 | Avaliar paralelização                 | Testar, em lote, `Parallel.ForEach` nas operações de leitura e agregação; garantir thread‑safety dos índices.                                                             | Variável (core functions)                                    | 0 %    |
| 17 | Documentar e publicar mudanças        | Escrever documentação de uso e de migração; explicar como habilitar/desabilitar recursos (por exemplo, cache de AST).                                                     | README / docs                                                | 0 %    |
| 18 | Criar testes unitários                | Adicionar cobertura unitária para cada otimização (índices, cache, JSON, temporal) garantindo que não quebre funcionalidades existentes.                                  | Projetos de testes                                           | 0 %    |
